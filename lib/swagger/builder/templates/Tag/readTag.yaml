/%PATH%/{id}:
  x-swagger-router-controller: %CONTROLLER%
  get:
    tags:
      - %TAG%
    operationId: %OPERATION%
    parameters:
      -
        name: id
        type: string
        in: path
        required: true
    responses:
      200:
        description: OK
        schema:
          $ref: "#/definitions/%MODEL%"
